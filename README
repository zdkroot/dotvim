Installation:

	git clone https://zdkama@github.com/zdkama/dotvim.git ~/.vim

Create symlinks:

	ln -s ~/.vim/vimrc ~/.vimrc
	ln -s ~/.vim/gimrc ~/.gvimrc

Update config on new machine

	cd ~/.vim
	git submodule init
	git submodule update

Update plugins

	cd ~/.vim/bundle/fugitive
	git pull



